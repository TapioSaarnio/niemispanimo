{
  "name": "niemispanimo",
  "version": "1.0.0",
  "description": "kotisivut",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node niemispanimobe",
    "build": "rmdir build /s && cd niemispanimofe && npm install && npm run build && move build .. && cd .. && git add . && git commit -m build && git push -u origin main && git push heroku master"
  },
  "author": "Tapio Saarnio",
  "license": "ISC",
  "dependencies": {
    "@google-cloud/storage": "^5.8.4",
    "bcrypt": "^5.0.1",
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^5.12.7",
    "mongoose-unique-validator": "^2.0.3",
    "multer": "^1.4.2"
  }
}
